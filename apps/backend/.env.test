# Test Environment Configuration
NODE_ENV=test

# Test Database (use main database with proper cleanup)
DATABASE_URL="postgresql://postgres:password@localhost:5432/pharma_control"

# Redis for tests
REDIS_URL="redis://localhost:6379/1"

# JWT Configuration for tests
JWT_SECRET="test-jwt-secret-for-pharmaceutical-system-testing-only"
JWT_EXPIRES_IN="24h"

# Test Admin Credentials
DEFAULT_ADMIN_PASSWORD="test-admin-password"
DEFAULT_QA_PASSWORD="test-qa-password"

# Port for test server
PORT=3001

# Elasticsearch for tests (optional)
ELASTICSEARCH_URL="http://localhost:9200"